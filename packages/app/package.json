{
  "name": "@webhack/app",
  "private": true,
  "version": "0.0.1",
  "license": "MIT",
  "main": "index.ts",
  "description": "Webhack example app",
  "scripts": {
    "build:dev": "NODE_ENV=developent webpack --mode development",
    "build": "NODE_ENV=production webpack -p --mode production",
    "start": "NODE_ENV=developent webpack-dev-server --hot --progress --colors -d -p --mode production"
  },
  "engines": {
    "node": ">= 8"
  },
  "dependencies": {
    "@webhack/dev": "*",
    "babel-regenerator-runtime": "6.5.0",
    "webpack": "4.1.1",
    "webpack-cli": "2.0.12",
    "webpack-dev-server": "3.1.1"
  }
}
