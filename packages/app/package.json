{
  "name": "@skeleton/app",
  "private": true,
  "version": "0.0.1",
  "license": "MIT",
  "main": "index.ts",
  "description": "Skeleton example app",
  "scripts": {
    "build:dev": "NODE_ENV=developent webpack --mode development",
    "compile": "tsc -p .",
    "build": "NODE_ENV=production webpack -p --mode production",
    "start": "NODE_ENV=developent webpack-dev-server --hot --colors -d -p --mode production"
  },
  "engines": {
    "node": ">= 8"
  },
  "dependencies": {
    "@skeleton/dev": "*",
    "babel-regenerator-runtime": "6.5.0",
    "typescript": "2.7.2",
    "webpack": "4.1.1",
    "webpack-cli": "2.0.12",
    "webpack-dev-server": "3.1.1"
  }
}
